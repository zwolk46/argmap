Begin a Work Session under the Constitution at `.claude/CLAUDE.md`. Execute Article VIII in full.

Onboard: confirm you've read the Constitution and Protocol, then read `docs/current_state.html`, `docs/flags.html`, `docs/stream_i_contracts_v2.html`, and the `src/<module>/README.md` files for any modules your work will touch. The original Stream A–H design docs and Stream I per-module spec docs were retired 2026-05-23; the per-module READMEs plus current_state are the post-implementation source of truth.

Identify the next work from my prompt below (or, if I haven't named one, propose the highest-ROI item from the unresolved entries in `docs/flags.html` and the open questions in `docs/current_state.html`).

Confirm in 3–5 sentences: (a) the work you've identified, (b) the README/contracts/flag entries you'll consult, (c) the dependency modules you'll read READMEs for, (d) anything you'll defer. Wait for my one-word confirmation ("yes" to proceed; anything else to discuss).

After confirmation: proceed through Work → Self-Review → Persistence → Handoff. Persistence writes code in `src/<module>/`, tests in `tests/<module>/`, updates the relevant `src/<module>/README.md` if the public surface changed, and updates `docs/current_state.html` and `docs/flags.html` per the Protocol.
