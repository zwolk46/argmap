Begin a Work Session under the Constitution at `.claude/CLAUDE.md`. Execute Article VIII in full.

Onboard: confirm you've read the Constitution and Protocol, then read `docs/current_state.html` and `docs/flags.html`. The buildkit index at `docs/stream_i_buildkit_index_v1.html` specifies the coding-session reading order — follow it.

Identify the next work: the next module in the buildkit's implementation-sequence table whose Stream I spec exists in `docs/` but whose `src/[module]/README.md` does not yet exist. If no `src/` directory exists yet, your work additionally includes scaffolding the I.1 skeleton first, per the buildkit's "Repo layout the skeleton ships" and "What ships in the I.1 skeleton" sections.

Confirm in 3–5 sentences: (a) the module you've identified, (b) the spec doc you're working from, (c) the dependency READMEs you'll read, (d) anything you'll defer. Wait for my one-word confirmation ("yes" to proceed; anything else to discuss).

After confirmation: proceed through Work → Self-Review → Persistence → Handoff. Persistence writes code in `src/[module]/`, tests in `tests/[module]/`, a one-screen `src/[module]/README.md`, and updates `docs/current_state.html` and `docs/flags.html` per the Protocol.
