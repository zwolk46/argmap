Branch: main. Primary directory: /Users/zacharywolk/zwolk/argmap. The branch-scoped Vercel deploy lacks API keys, so the user has consented to direct main-branch deploys; per-session worktrees still isolate the harness, then merge to main and deploy from the primary directory. Test count: 1757.

**Coding session #20 (2026-05-21) — overhaul merge + bug-fix port.** The I.10b shadcn/Phosphor/Tailwind-v4 UI overhaul (branch `worktree-ui-overhaul-shadcn-20260520`, commits b697064..11a2ab2) was merged into `main` per explicit user authorization, then the 14 bug fixes from the parallel `worktree-bugfix-ui-20260520` session (originally on `main` at 9a6c516 but written against the pre-overhaul primitives) were re-applied onto the new shadcn substrate. Each fix's port is recorded in F-030. All worktrees (`audit-fixes-20260516`, `audit-f09-f10-20260519`, `audit-a11y-canvas-kbd-20260520`, `ui-overhaul-shadcn-20260520`) are now stale and may be removed when convenient.

**Manual verification needed on deployed build:**

1. **CSP (§16 F-28, carry-forward):** Open https://audit-fixes-20260516.vercel.app in a browser with DevTools Console open and walk: sign-in (Supabase + Google Fonts), Frame Building canvas (ELK worker), AI hook flow (Anthropic API). Any console line starting with "Refused to … because it violates …" indicates the CSP needs widening in vercel.json. Curl confirmed all five headers (CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) are served correctly — only runtime CSP compliance remains to confirm.

2. **SessionSettings backdrop + MetadataSection commit-on-blur ordering (carry-forward, from §9 #25):** Open Session settings, type a draft title or description, then click the new dark backdrop scrim outside the panel. Expected: the draft commits (via the blur handler) BEFORE the panel closes. If the title reverts to the prior value after backdrop click, the event order is wrong on this browser and we need to move the commit to an explicit `mousedown` capture. Cannot be reproduced without a real DOM/browser, so it stays manual.

3. **§12 F-18/F-20/F-28 (carry-forward):** The suggestion-drawer changes only fire when `ai_hooks_enabled` is true — `RepositoryProvider` still hard-codes `false` (`src/state/context.tsx:117`) because no LLM runner is wired into production. Tests cover the new behaviors at unit level (16 drawer tests, +4 store/hook tests). When AI is wired live, manually verify: (a) accepting a G2 interpretation in legal mode shows the Authority + CITES side-effects in the "Changes that will be applied" section before commit; (b) clicking Reject during applying shows "Cancelling…" on Reject and "Working…" on the other two; (c) the header chip reads e.g. "CHECKPOINT" rather than "G1".

4. **§12 F-21 (NEW, this session):** When AI is wired live, manually verify that G2/G9/G11 suggestion drawers render at ~640px (wider than other hooks). Pure helper logic is unit-covered; the rendered width is mediated by the Drawer primitive's `width` prop, which is identical to its prior usage shape.

5. **§8 #1 legacy data backfill (carry-forward):** Any pre-fix ArgumentSessionVersion rows in production have no `frame_version_snapshot` and still fall back to the live session's snapshot (i.e., the prior "visual lie" for those specific historical rows). Write paths now auto-populate the field on every save, so legacy rows self-heal the first time they're re-saved (e.g., on a milestone re-mark or via the saveSessionVersion backfill). If production data exists with historical ASVs that should be retroactively snapshotted, a one-time backfill script could load every ASV and call saveSessionVersion — the existing backfill in saveSessionVersion will populate the snapshot from the parent session. Not yet planned because production deploy is still gated and no real session data exists upstream of this fix.

6. **§13 #4–#6 canvas keyboard nav (NEW, this session):** Tab into the canvas, focus a node, then verify with both a keyboard and a screen reader: (a) SR announces e.g. "Sub-question: <statement>" instead of the generic "node"; (b) arrow keys move the selected node 1px (Shift+arrow 4px) — RF's built-in behavior; (c) clicking a palette item drops a new node and focus lands on it (look for the accent focus ring around the new node); (d) pressing E with a node focused opens the "Edge mode — …" banner at the top of the canvas; arrow keys / Tab cycle candidate targets (the dashed ring on a candidate); Enter routes through the existing edge-creation popup; Escape cancels. Unit tests cover the props + capture-phase handler logic, but the real RF wrapper, focus styling, and SR pronunciation can only be confirmed in a browser.

**Background coding sessions can't open a browser, so these need a human (or a session with browser access).**

Audit source of truth: /Users/zacharywolk/zwolk/argmap/.claude/worktrees/audit-report-20260516/.claude/audit/2026-05-16/ (15 per-area markdown files: 01-boot-auth.md through 16-performance-misc.md).

What's already done (do not re-fix)

Every CRITICAL across §1–§16, and the HIGHs in each section that didn't require schema/data migration, architectural rewrite, or cross-store coupling. Specifically the headlines:

- §4 canvas P0-3, P0-4, P0-6, P0-7, P0-9, P0-11
- §5 panes P0-5, P0-6
- §6 argument-running C3, H4, H9, H14, H16, M16, L2, L8
- §7 mode-change H1, H7, M9, M14
- §8 version-history #1, #2 (frame restore session-count advisory), #4, #6, #7, #8, #9, #10, #11, #12
- §9 chrome HIGH #1–#6, #11, #27, #28, #29 + MEDIUM #7, #8, #10, #12, #13, #14, #15, #17, #19, #20, #21, #24, #25, #30, #32, #33, #34, #36, #39, #40
- §11 persistence H-10, H-11, H-12, H-14, H-16 + new typed RepositoryError.code
- §12 LLM hooks F-03, F-04, F-05 (frame + session halves both done), F-07, F-09 (G6 rewrite provenance — output_overrides.rewrite_invocation carries the record, prose-tab chip reads it via OutputViewPayload), F-10 (G6 rewrite-then-rewrite explicit baseline — prose-tab "Refine rewrite" / "Rewrite from canonical", G6.buildInput parses { baseline, baseline_kind }, drawer subtitle, fixes latent String({canonical}) crash), F-11, F-13, F-15, F-16, F-17, F-18 (CommitPlan preview before Accept), F-19 (dead SuggestionEditPanel deleted), F-20 (specific in-flight copy on Accept/Reject/Edit + aria-busy), F-21 (per-hook drawer width — G2/G9/G11 wide), F-22, F-28 (human hook short name in drawer header chip) — schema_out enforced by runHook before commit, SuggestionResult and HookInvocationRecord carry prompt_body_hash + rendered_prompt_hash; the per-frame LlmSettings gate runs in useAiSuggestion for session-store hooks via session_store.rejectSuggestion(reason); ParseAssertError separates parse-time asserts from real provider errors; AiAttributionChip accepts an optional record + hook_id and the prose-tab G6 rewrite header now renders the shared chip; SuggestionDrawer shows a grouped CommitPlan preview ("Create N Interpretation nodes" / "Update N fields" with per-write previews and the "new version" notice) computed via the new `preview_commit` opt threaded through frame_store + session_store + useAiSuggestion
- §13 a11y HIGH #4, #5, #6 (this session — canvas keyboard nav: per-node ariaLabel + wrapper landmark + focus ring; palette click → select + focus the new node so RF arrow-key positioning applies; "E" edge-creation mode with arrow-cycle candidate targets, Enter to connect, Escape to cancel), #7, #8, #10, #11, #12, #13, #14 (tab-order predictability subsumed by #4), #15, #16, #17, #18
- §14 visual design HIGH #6, #7, #11, #27, #33
- §15 schema F-3, F-4, F-5, F-7, F-8, F-9, F-10, F-11, F-12, F-14, F-15 (new V-ARG-9, V-NODE-10, V-NODE-11, V-NODE-12, V-EDGE-5, cross-Frame V-FR-10 wired at frame-store load/restore + runFrameAction, transition advisories)
- §16 perf F-07 (README), F-08 (smoke already correct), F-28 (vercel.json: CSP + X-Frame-Options + X-Content-Type-Options + Referrer-Policy + Permissions-Policy) — headers verified by curl; runtime walk still pending

Material still to resolve — ordered by ROI

Architectural / data layer (largest):

1. §11 C-1..C-5 persistence overhaul. No IndexedDb fallback at runtime (src/main.tsx:96-99 only wires SupabaseRepository), failed autosaves never retry (src/persistence/autosave.ts:175-187), pagehide/beforeunload flush is fire-and-forget (src/main.tsx:117-140), composite repo methods (createBlankFrame, migrateSession, restoreFrameVersion in src/persistence/supabase-repository.ts) do 2-N sequential writes with no transaction, and optimistic-UI mutations never rollback on save failure. Audit recommends sendBeacon for unload-time flushes, Postgres RPC for composite atomicity, and a dirty-flag-banner-plus-rollback path in src/state/frame-store.ts + session-store.ts applyPatch handlers.
2. §11 H-13/H-15/H-17/H-19 search + pagination + cross-store broadcast. search_index.tsv is null permanently (Postgres-side generated tsvector column needed). listFrames / listFrameVersions / listSessionVersions have no pagination — users with 1000+ rows lose recent history. deleteFrame doesn't publish frame_deleted; peer tabs stay stale. App-state broadcasts can ping-pong across tabs.
3. ~~§8 #1 ArgumentSessionVersion own frame_version_snapshot — DONE in commit 8360b99~~.
4. ~~§8 #2 destructive frame-restore disclosure — disclosure half DONE in commit f2c2774~~. Per-session migration prompts (the other audit recommendation) still pending; would require routing the affected session_id list into a per-session "migrate to new head?" prompt and probably a small `migrateSession` opt to target the new frame head version. Lower ROI now that the disclosure exists. Skip unless user prioritizes.

LLM determinism / auditability (remaining):

5. ~~§12 F-09 — DONE in commit a1dd9f6~~. ArgumentSessionVersion.output_overrides.rewrite_invocation now carries the HookInvocationRecord; AiAttributionChip in prose-tab reads it via OutputViewPayload.prose.rewrite_invocation. Populated by the apply_decision shim when wired live (not yet wired in main.tsx).
6. ~~§12 F-10 — DONE in commit a1dd9f6~~. Prose-tab renders two discrete buttons ("Refine rewrite" / "Rewrite from canonical") when a rewrite exists. G6.buildInput now reads { baseline, baseline_kind } structured payload (with string back-compat). SuggestionDrawer renders an explicit baseline subtitle for G6. Also fixed the latent `String({canonical})` → "[object Object]" crash.
7. ~~§12 F-21 drawer width — DONE in commit ffb378f~~. G2/G9/G11 render at min(640px, 100vw); all other hooks unchanged at min(420px, 100vw). F-15/F-16/F-17/F-18/F-19/F-20/F-21/F-22/F-28 are all done now. F-09 + F-10 are also done as of a1dd9f6 — the §12 audit's HIGH/MEDIUM list is exhausted for non-architectural items.

A11y / UX (mid-size):

8. ~~§13 #4–#6 canvas keyboard nav — DONE in commit b54fab5 / merge 3a872ad~~. RF v12 already defaults `nodesFocusable: true` with per-node `tabIndex=0`, Enter/Space → select, arrow keys → move-selected-node, Escape → unselect. The audit's "off-keyboard" premise was partially stale. This session added: per-node ariaLabel ("<humanized type>: <primary_text>"), wrapper aria-label describing the keyboard model, focus-visible ring on .canvas-node, palette-click → setSelection + focus on the new [data-id] wrapper (mirrored in on_palette_drop), and an entirely new keyboard edge-creation mode ("E" from a focused node enters mode; arrow/Tab cycle candidate targets; Enter routes through existing edge-creation popup; Escape cancels; banner + data-edge-source/data-edge-candidate attrs + matching CSS rings). #14 (tab order predictability) effectively subsumed.
9. §14 #1 mode-accent transition. Mode flip currently snaps colors in one frame. Deferred because it's a broad CSS change with unknown layout side-effects without browser verification; pick a small set of consumer elements (focus rings, button accents, edge colors) and add a 150ms transition.
10. §9 #38 session-settings panel read-only/preview gating. **Defensive — no current caller renders SessionSettingsPanel in a read-only surface.** The panel is only mounted from argument-running-page; version-history's session-preview-view doesn't open it. Per the audit the fix would add a `read_only` prop and gate the editing controls, but until there's an actual preview surface that mounts the panel this is YAGNI per CLAUDE.md. Reconsider when §8 #1 (session preview rewrite) lands; that work may legitimately need a read-only session-settings surface.

Schema / Other:

11. §15 F-6 Premise.kind discriminated union. **DEFERRED** — V-ARG-3 runtime already validates the cross-vocabulary leak, but the static type is a wide union. A real static guard requires either (a) splitting Premise into per-mode-flavor sub-types with consequence at every call site, or (b) carrying mode_flavor on each Premise (denormalization). Both break the Premise contract widely; the call is a schema design decision that warrants user input or its own focused session. Per Article IV § 4 substantial-hesitation standard.
12. §15 F-13 `linked_to` SubQuestion-parent check. **AMBIGUOUS — needs user input before implementing.** The audit header (15-schema-edges.md:121) says "Terms share the same SubQuestion parent," but the audit body describes the contract as "shared interpretations across SubQuestions" — the literal text says `linked_to` is meant to connect Terms ACROSS different SubQuestions, which the same-parent rule would forbid. Two defensible rules: (a) enforce same-SubQuestion parent (header reading; restrictive); (b) enforce that linked Terms have matching `INTERPRETED_AS` child sets (body reading; preserves the cross-SubQuestion feature). Pick before writing the new V-NODE rule. Per Article IV § 4 substantial-hesitation standard.

Polish / dead-code:

13. §16 F-13 audit-code rationale comments. // P0-N, // P1-N, // F-NNN markers leak into ~25 source files. Mechanical sweep. Don't touch unless you're already touching that file for a real fix — orphan diffs aren't worth the review burden.
14. §16 F-26 error-boundary remote reporting. src/ui/error-boundary.tsx:24 logs to console only. Add Sentry / equivalent (out of scope for non-architectural pass).

Working notes for the next agent

- The codebase is on worktree-audit-fixes-20260516, NOT main. Main is unchanged at c380c71 (last commit: F-028 snapshot). Before continuing, either git checkout worktree-audit-fixes-20260516 from main repo, or work directly in /Users/zacharywolk/zwolk/argmap/.claude/worktrees/audit-fixes-20260516. The user has not asked for a merge to main yet; do not push or merge without explicit consent.
- Run npm test && npm run typecheck && npm run lint && npm run format -- --check from the worktree dir before each commit. All four were green at 751e2ce. Test count: 1704.
- CLAUDE.md determinism constraints are still binding: F-028 snapshot fields on FrameVersion, LLM model pinned to claude-3-7-sonnet-20250219 with temperature: 0, no dispose() on useMemo([]) resources (StrictMode), detectSessionInUrl: false in supabase-client.ts.
- Use useOptionalToast (not useToast) in any primitive that may render in test harnesses without <ToastProvider>. Pattern established in chrome/frame-title.tsx, version-history/restore-confirm-dialog.tsx, and session-settings/archive-delete-section.tsx.
- ESLint module boundaries: src/ui/ may only type-import from @/llm-hooks (no value imports). src/state/ may not import @/llm-hooks at all (not even type imports). If you need hook metadata at the UI layer (e.g., activation strings, mode_visibility), either pass it through @/schema or hardcode the minimum needed (see use-ai-suggestion.ts gate which uses both runtime + output-time group flags coarsely rather than activation lookup). New: hookShortName is exported from src/ui/primitives — UI surfaces can import it for human-readable hook labels.
- New helpers introduced in earlier sessions: src/llm-hooks/schema-validate.ts (JSON-schema subset validator covering type, required, properties, items, enum, minLength/maxLength, minItems/maxItems, minimum/maximum), and src/schema/validation-rules.ts::validateFrameVersionAgainstFrame (cross-Frame V-FR-10 extension). validateOnly in src/state/action-runner.ts now takes an optional Frame to merge cross-Frame results.
- session_store.rejectSuggestion(reason: string) is the surface for "the per-frame gate denied this hook" — keeps session_store agnostic of frame state while still letting the toast bridge render the failure.
- InlineAlert accepts `id` so a form input's aria-describedby can point at it. Pattern: per-field useId for both error and hint span ids; combine with `.filter(Boolean).join(" ")` for multi-source describedby.
- MetadataSection (session-settings) uses a ref-guarded resync effect so cross-tab realtime updates don't trample uncommitted drafts. If you copy this pattern to FrameTitle or any other commit-on-blur input, mirror the `last_synced_*` ref.
- Drawer primitive accepts an opt-in `show_backdrop` prop (§9 #25). When true it renders a semi-transparent scrim at `Z.drawer - 1 = 79` and routes scrim clicks to `onClose`. Currently enabled on HelpGlossaryPane and SessionSettingsPanel. NOT enabled on SuggestionDrawer (async LLM work; accidental scrim clicks would discard it). When adding it elsewhere, audit the panel for commit-on-blur inputs — see the SessionSettings manual-verification note above.
- AiAttributionChip (§12 F-22) accepts EITHER `record: HookInvocationRecord` (rich tooltip with Hook/Prompt/Model/Generated) OR `hook_id: string` (chip only, no tooltip). The prose-tab G6 rewrite header passes both: it threads `record={payload.prose.rewrite_invocation ?? null}` AND `hook_id="G6"` so the rich tooltip lights up automatically when output_overrides.rewrite_invocation gets populated (§12 F-09 wiring is in place — the apply_decision shim is responsible for writing the slim record there).
- **NEW (this session) §12 F-18 — CommitPlan preview**: frame_store and session_store accept an optional `preview_commit?: (hook_id, suggestion, decision) => unknown` opt that returns a CommitPlan. The store action `previewCommit(decision)` returns null when there's no pending suggestion, returns `{ writes: [], versioned: false }` for rejected decisions without calling preview_commit, and routes accepted/edited through the opt. useAiSuggestion exposes `previewCommit(decision): CommitPlan | null` (casts the unknown). SuggestionDrawer computes a preview decision based on edit state, calls previewCommit, and renders a grouped "Changes that will be applied" section with per-group counts, type labels, and up-to-6 human previews (statement / name / label) plus a "+N more" line. Wire `preview_commit` in main.tsx alongside `apply_decision` when AI hooks go live.
- **NEW (this session) §12 F-20 — drawer in-flight feedback**: SuggestionDrawer tracks `inflight: "accepted" | "edited" | "rejected" | null` set in the click handlers and cleared via an effect on `is_applying`. The three buttons display specific in-flight copy (Applying… / Saving… / Cancelling…) on the triggering button and a neutral "Working…" on the other two. An aria-busy wrapper around the buttons (data-testid="suggestion-footer-buttons") signals state to assistive tech.
- **NEW (this session) §12 F-28 — drawer header chip**: the chip now uses `hookShortName(result.hook_id)` so "G1" reads as "CHECKPOINT", "G2" as "INTERP", etc. The chip styling still uppercase-monospaces the label so it reads as a category tag.
- **NEW (this session) §12 F-19 — dead code removed**: src/ui/ai-suggestion/suggestion-edit-panel.tsx is gone. SUGGESTION_EDIT_PANELS registry was empty and SuggestionEditPanel was never used by the drawer.
- The audit reports themselves live in a separate worktree (worktree-audit-report-20260516) so they survive across branches. Don't edit them.

Deployment is live at https://audit-fixes-20260516.vercel.app (branch-aliased); redeploy after each substantial batch with vercel deploy --prod --yes --scope zachs-projects-74dd78e7 from the worktree dir.

Session log (2026-05-19):

- Commit 5b5cb6b: §12 F-18 CommitPlan preview before Accept. Adds preview_commit opt to both stores, previewCommit action, useAiSuggestion exposure, and a human-readable grouped summary in the drawer body ("Create 3 Interpretation nodes" / "Update 1 field" / "+N more"). +15 tests, 1685 → 1700. Deploy: dpl_9qqggG8EwPYF3YJzVLB8vkGKL3Kd.
- Commit 751e2ce: §12 F-19 + F-20 + F-28 drawer polish. Deleted dead suggestion-edit-panel.tsx. Inflight state drives specific Applying… / Cancelling… / Saving… copy with "Working…" on idle siblings. Header chip uses hookShortName. +4 tests, 1700 → 1704. Deploy: dpl_6gtYmir8cM1dxLWvyJfZ66mnMx7i.
- Commit 8360b99: §8 #1 frame_version_snapshot captured on every ArgumentSessionVersion write. New optional schema field; backfill in saveSessionVersion (both repos); migrate snapshots target frame; restore snapshots live session's frame (NOT ancestor's, since restore preserves session.frame_version_id); SessionPreviewView prefers the version's own snapshot. diff.ts uses an allowlist that excludes the field, so compare view stays clean. Envelope schema_version unchanged (field is optional). +9 tests, 1704 → 1713. Deploy: dpl_8ecCMGMe8g9TKY2iYqHyLDBN1eLb.
- Commit ffb378f: §12 F-21 per-hook drawer width. G2/G9/G11 render at min(640px, 100vw); rest unchanged at min(420px, 100vw). Exports `suggestionDrawerWidth(hook_id)` from suggestion-drawer; declared in src/ui/ (not on HookContract) to respect the module-boundary rule. +3 tests, 1713 → 1716. Deploy: dpl_7Vr5HWY9M912FAJcyCLkEm4qeJc1.
- Commit f2c2774: §8 #2 frame-restore session-count advisory. RestoreConfirmDialog accepts an optional frame_id, fetches listSessionsForFrame on open (entity_kind="frame" only), and renders an advisory paragraph below the standard branching copy when 1+ sessions are anchored. Both repos already exclude archived rows in listSessionsForFrame so the count reflects live sessions only. Fetch failure suppresses the advisory rather than blocking the restore. +8 tests, 1716 → 1724. Deploy: dpl_F6cAJpfcXTFzpVgmBeoNJAgDtfaR (production, via main; branch-scoped env was missing API keys so the user requested a main-branch deploy this session).
- Commit a1dd9f6 / merge c6cb535: §12 F-09 + F-10. (F-09) ArgumentSessionVersion.output_overrides gains an optional rewrite_invocation: HookInvocationRecord; OutputViewPayload.prose threads it through; prose-tab's AiAttributionChip reads `record={payload.prose.rewrite_invocation ?? null}` so the rich tooltip lights up the moment the apply_decision shim writes the field. (F-10) Prose-tab now offers two discrete buttons when a rewrite exists — "Refine rewrite" (baseline = prior rewrite) and "Rewrite from canonical" (baseline = canonical). G6.buildInput parses { baseline, baseline_kind } (string back-compat retained), fixing the latent `String({canonical})` → "[object Object]" crash. SuggestionDrawer renders a baseline subtitle for G6 from echo_input.baseline_kind. README in src/ui/argument-running updated to reflect the new chip wiring. +13 tests, 1724 → 1737. Deploy: dpl_7gfzWkfosjKnkJmt2p2NaGNcyEAa.
- User-consented (2026-05-19): merge worktree-audit-fixes-20260516 into main and deploy from /Users/zacharywolk/zwolk/argmap (primary directory) rather than the worktree, because the branch-scoped Vercel deployment lacked the API keys needed for live testing.

Session log (2026-05-20):

- Commit b54fab5 / merge 3a872ad: §13 #4–#6 canvas keyboard nav. (#4) RFNode ariaLabel="<humanized type>: <primary_text>" (truncated 117+ellipsis); wrapper aria-label + aria-describedby span describing the keyboard model; `:focus-visible` accent ring on .canvas-node when the RF wrapper has keyboard focus. (#5) NodePalette accepts an optional on_node_created callback; FrameBuildingPage uses it to setSelection({kind:"node",…}) and rAF-deferred .focus() on the new [data-id] — mirrored in on_palette_drop. RF's built-in arrow-key positioning then applies. (#6) New canvas-local state { source, candidate_index }: "E" with a focused node enters mode (capture-phase keydown listener — bypasses RF arrow-key node movement); arrow/Tab cycle non-source RF nodes as candidate targets; Enter calls on_edge_created which routes through the existing edge-creation popup; Escape cancels and restores focus to the source. data-edge-source / data-edge-candidate paint solid + dashed accent rings; cleared on commit/cancel. role=status banner with candidate count. +19 tests (17 frame-canvas-keyboard.test.tsx + 2 node-palette.test.tsx), 1737 → 1756. Deploy: dpl_46DHwTAZhL52SsiRUFMYxU8nJM4H.

Session I.10b (2026-05-20) — UI overhaul (shadcn/ui + Phosphor):

- Branch: `worktree-ui-overhaul-shadcn-20260520`. Worktree at `/Users/zacharywolk/zwolk/argmap/.claude/worktrees/ui-overhaul-shadcn-20260520`. Three commits landed; not merged to main; not auto-deployed.
- Commit b697064 — foundation. Added tailwindcss + @tailwindcss/vite v4, @phosphor-icons/react, radix-ui v1.4.3 (umbrella), class-variance-authority, clsx, tailwind-merge, tw-animate-css, @fontsource-variable/inter, sonner, next-themes, prettier-plugin-tailwindcss, shadcn (dev). Generated 25 shadcn primitives under src/components/ui/. Added src/lib/utils.ts (`cn()`), src/index.css (Tailwind imports + @theme inline bridge + OKLCH neutral palette in :root + .dark + @imports of existing argmap CSS), components.json (style radix-maia, baseColor neutral, iconLibrary phosphor), .mcp.json (shadcn MCP). Updated tsconfig.json (resolvePackageJsonImports), vite.config.ts (tailwindcss() plugin), package.json (added `imports` for #components/_ / #lib/_ / #hooks/_ / #ui/_; existing @/ aliases retained), index.html (dropped 3 UICONS CDN <link>s + 1 Google Fonts Inter link; kept Source Serif 4 + JetBrains Mono), src/ui/styles/tokens.css (font-family line only — now prefers "Inter Variable").
- Commit 0a8dc9f — primitives + chrome. Migrated ~25 primitive wrappers + ~10 chrome files to shadcn-backed implementations, preserving public exports and prop shapes. Consequence: button shape is now pill (rounded-4xl in radix-maia); Dialog / Drawer (now backed by shadcn Sheet) / Tooltip / Popover / Dropdown / Toast use Radix-portaled DOM (tests that previously walked the container subtree updated to `screen.*` / `document.body.*` queries).
- Commit c92a64c — surfaces + canvas re-skin. ~150 TSX files migrated across home, auth, onboarding, frame-building (non-canvas), argument-running (non-canvas), version-history, session-settings, mode-change, session-migration, and canvas (3 files only — re-skin with Phosphor + minor restyling; .canvas-node\* CSS in global.css that carries the five-status treatment is untouched). Added docs/stream_e_amendment_1.html (Stream E v1 amendment per Article XI § 1) + docs/handoff/ui_overhaul_mapping_v1.md (planning artifact). 10 test files updated for portal-query / role-attribute / variant-attribute migration. OutputViewTabs reverted from shadcn Tabs to raw role-based buttons (Radix Tabs doesn't fire onValueChange under happy-dom — visual contract identical to <Tabs variant="line">).
- Three visual deltas that supersede earlier in-code defaults (recorded in F-029 — review-session targets if user objects): (a) AI suggestion drawer renders a scrim (shadcn Sheet always overlays bg-black/80; the prior argmap Drawer honored show_backdrop=false specifically on SuggestionDrawer — functionally acceptable now since onClose does not discard the suggestion, Reject button remains canonical cancel); (b) version-history preview banner reverted to not-applicable wash per spec D (an earlier deliberate move to warning-gold for discoverability is undone); (c) OutputViewTabs raw-button implementation noted above.
- Verification: typecheck clean; `npm run build` clean (CSS 152 kB; main JS chunk +30 kB for Phosphor + Radix umbrella + transitional UIcon shim); vitest 1757/1757 across 163 files (+1 over the 1756 baseline); lint clean; prettier clean; ESLint determinism plugin tests clean; iteration-order audit clean; Playwright e2e 8 passed / 2 skipped (env-gated).
- Deploy status: **NOT deployed** per the CLAUDE.md overhaul exception. Branch is ready for user review + explicit deploy authorization. Dev server: `npm run dev` at http://localhost:5173 for local inspection. After authorization, deploy with `vercel deploy --prod --yes --scope zachs-projects-74dd78e7` from the main worktree dir after the branch is merged.
- Deferred (safe; not blocking any next session):
  - UIcon shim removal. `src/ui/primitives/uicon.tsx` is still a Phosphor-backed slug→glyph lookup consumed by ~25 surfaces. The clean follow-up: replace each `<UIcon name="…" />` call with a direct Phosphor import, then delete the shim. Obvious next pass if the user wants to fully delete the legacy slug surface.
  - Dark theme. The preset's `.dark` palette block is present in src/index.css but not activated. Stream E v1 light-only commitment stands.
  - Responsive / mobile / narrow-screen reflow. Top-bar chips still hide at 720/560/480px breakpoints; the three-pane / two-pane workspaces do not reflow.
  - Full WCAG 2.1 AA audit and print stylesheet. Both remain on the Stream E / I deferral list. Radix accessibility baseline is in place via the shadcn primitives, but no AA-criteria audit has been performed.
  - `.argmap-*` global utility class prune. Still loaded in src/ui/styles/global.css; the canvas uses `.canvas-node*` selectors and several transitional surfaces still reference others. Prune unreferenced classes when surface migration is fully complete. NOT urgent.
- Open follow-ups the next session can pick up safely (none blocking): UIcon shim removal is the obvious next pass.
- Persistence artifacts written this handoff: docs/current_state.html updated (date + Stream E + Stream I status rows + Open questions amended + I.10b session #19 prose block prepended to the Coding phase handoff + I.10b entry in the coding session sequence + Stream E amendment pointer-index entry); docs/flags.html updated (subtitle bumped, next-id bumped to F-030, F-029 table row + full record appended); HANDOFF_NEXT.md (this section).
