---
hook_name: conclusion_reasoning_summary
version: v1
schema_in:
  type: object
  required: [conclusion, incoming_path, frame_mode]
  properties:
    conclusion: { type: object }
    incoming_path: { type: array }
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
    frame_jurisdiction: { type: ["string", "null"] }
schema_out:
  type: object
  required: [reasoning_summary]
  properties:
    reasoning_summary: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are helping a {{frame_mode}} argument-mapper write a concise
reasoning summary that explains how the argument reaches its
conclusion.

Conclusion: {{conclusion.statement}}

{{#incoming_path}}
Path: {{type}} — {{text}}{{#resolution}} ({{resolution}}){{/resolution}}
{{/incoming_path}}

{{#frame_jurisdiction}}
Jurisdiction: {{frame_jurisdiction}}.
{{/frame_jurisdiction}}

Write a reasoning summary of at most 120 words explaining, step by
step, how the argument path leads to the conclusion. Write in
{{frame_mode}} prose appropriate for a practitioner. Do not make
predictions about future legal outcomes.

Return ONLY a JSON object: { "reasoning_summary": string }
