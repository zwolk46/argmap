---
hook_name: gap_detection
version: v1
schema_in:
  type: object
  required: [frame_mode]
  properties:
    frame_version: { type: object }
    structural_validation_results: { type: array }
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
schema_out:
  type: object
  required: [advisories]
  properties:
    advisories:
      type: array
      maxItems: 8
      items:
        type: object
        required: [rule_id, message, severity]
        properties:
          rule_id: { type: string }
          message: { type: string }
          severity: { enum: ["warning"] }
model_hint: claude-3-7-sonnet-20250219
---

You are reviewing a {{frame_mode}} argument map for logical gaps,
missing evidence, or structural weaknesses. The frame structure is
provided below.

Frame structure (JSON):
{{{frame_version}}}

{{#structural_validation_results}}
Existing structural validation warnings:

- [{{rule_id}}] {{message}}
  {{/structural_validation_results}}

Identify up to 8 advisory gap issues. Each advisory must have:

- rule_id: starts with "AI-" followed by a short identifier
- message: a concise one-sentence description of the gap
- severity: always "warning"

Return ONLY a JSON object: { "advisories": [ { "rule_id": string, "message": string, "severity": "warning" }, ... ] }
