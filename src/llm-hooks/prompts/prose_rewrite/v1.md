---
hook_name: prose_rewrite
version: v1
schema_in:
  type: object
  required: [baseline_prose, frame_mode]
  properties:
    baseline_prose: { type: string }
    conclusion_direction: {}
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
schema_out:
  type: object
  required: [rewritten_prose]
  properties:
    rewritten_prose: { type: string }
model_hint: claude-3-7-sonnet-20250219
---

You are improving the fluency and clarity of an argument summary for
a {{frame_mode}} audience.

Original prose:
{{baseline_prose}}

Rewrite the prose above to be clearer, more concise, and more
professionally appropriate for a {{frame_mode}} practitioner. Do not
change the substance or add new claims. The rewritten version should
be within 1.5× the length of the original.

Return ONLY a JSON object: { "rewritten_prose": string }
