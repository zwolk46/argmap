---
hook_name: checkpoint_question_generation
version: v1
schema_in:
  type: object
  required: [parent_subquestion, sibling_checkpoints, has_siblings, frame_mode]
  properties:
    parent_subquestion: { type: object }
    sibling_checkpoints: { type: array }
    has_siblings: { type: boolean }
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
    frame_jurisdiction: { type: ["string", "null"] }
schema_out:
  type: object
  required: [question, options]
  properties:
    question: { type: string, minLength: 5 }
    options:
      type: array
      minItems: 2
      maxItems: 6
      items:
        type: object
        required: [label]
        properties:
          label: { type: string, minLength: 1 }
          target_hint: { type: ["string", "null"] }
model_hint: claude-3-7-sonnet-latest
---

You are helping a {{frame_mode}} argument-mapper draft a Checkpoint
question that resolves under the parent SubQuestion below. A Checkpoint
is a single yes/no, multiple-choice, or graded question whose answer
routes the argument forward.

Parent SubQuestion: {{parent_subquestion.statement}}

{{#has_siblings}}
Sibling Checkpoints under the same SubQuestion:
{{#sibling_checkpoints}}

- {{question}}
  {{/sibling_checkpoints}}
  {{/has_siblings}}

{{#frame_jurisdiction}}
Jurisdiction: {{frame_jurisdiction}}.
{{/frame_jurisdiction}}

Constraints:

- The question must ask exactly ONE thing. If you find yourself wanting
  to write "X and Y" or "X, including Y," propose two Checkpoints
  instead and pick the most central one for this slot.
- Phrase the question in the idiom of the {{frame_mode}} domain.
- Each option must be a short label (a few words at most).
- Do not invent target node ids; leave target_hint as null unless a
  sibling Checkpoint's question is structurally an answer to a specific
  option, in which case use that sibling's id.

Return ONLY a JSON object matching this shape:
{ "question": string, "options": [ { "label": string, "target_hint": string | null }, ... ] }
