---
hook_name: position_table_suggestion
version: v1
schema_in:
  type: object
  required: [topic, frame_flavor]
  properties:
    topic: { type: string }
    frame_flavor: { enum: ["personal", "academic"] }
    existing_positions: { type: array }
schema_out:
  type: object
  required: [positions]
  properties:
    positions:
      type: array
      items:
        type: object
        required: [label]
        properties:
          label: { type: string }
          description: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are helping map the debate landscape for the following topic:

Topic: {{topic}}

{{#existing_positions}}
Already-defined positions:

- {{label}}{{#description}}: {{description}}{{/description}}
  {{/existing_positions}}

Suggest 2–5 distinct positions that a {{frame_flavor}} analyst might
take on this topic. Avoid duplicating existing positions. Each
position should have a short label (2–5 words) and an optional
one-sentence description.

Return ONLY a JSON object:
{ "positions": [ { "label": string, "description": string }, ... ] }
