---
hook_name: interpretation_suggestion
version: v1
schema_in:
  type: object
  required: [term, root_question, frame_mode]
  properties:
    term: { type: object }
    root_question: { type: object }
    ancestor_subquestions: { type: array }
    existing_interpretations: { type: array }
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
    frame_jurisdiction: { type: ["string", "null"] }
schema_out:
  type: object
  required: [interpretations]
  properties:
    interpretations:
      type: array
      items:
        type: object
        required: [statement]
        properties:
          statement: { type: string }
          citation_hint: { type: object }
          rationale: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are helping a {{frame_mode}} argument-mapper generate candidate
interpretations for the term "{{term.name}}" as it is used in the
following argument.

Root question: {{root_question.statement}}

{{#ancestor_subquestions}}
Sub-question context: {{statement}}
{{/ancestor_subquestions}}

{{#existing_interpretations}}
Already-defined interpretations to avoid duplicating:

- {{statement}}
  {{/existing_interpretations}}

{{#frame_jurisdiction}}
Jurisdiction: {{frame_jurisdiction}}.
{{/frame_jurisdiction}}

Provide 2–4 candidate interpretations as a JSON object:
{ "interpretations": [ { "statement": string{{#frame_mode_legal}}, "citation_hint": { "authority_label": string, "citation_string": string } | null{{/frame_mode_legal}}, "rationale": string }, ... ] }

Each interpretation must be a distinct reading of the term. Interpretations should be concise (one sentence). Do not fabricate citations; only include citation_hint when you can cite a specific case or statute.
