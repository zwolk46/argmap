---
hook_name: premise_drafting_from_fact_pattern
version: v1
schema_in:
  type: object
  required: [fact_pattern_text, target_checkpoint, frame_mode]
  properties:
    fact_pattern_text: { type: string }
    target_checkpoint: { type: object }
    frame_mode: { enum: ["legal", "general"] }
    frame_flavor: { enum: ["personal", "academic", null] }
    premise_kind_vocabulary: { type: array }
schema_out:
  type: object
  required: [draft_premises]
  properties:
    draft_premises:
      type: array
      items:
        type: object
        required: [statement, kind]
        properties:
          statement: { type: string }
          kind: { type: string }
          source_quote: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are extracting premises from the following fact pattern to support
a {{frame_mode}} argument.

Fact pattern:
{{fact_pattern_text}}

Target checkpoint question: {{target_checkpoint.question}}

{{#premise_kind_vocabulary}}
Valid premise kinds: {{.}}
{{/premise_kind_vocabulary}}

Extract 1–5 draft premises relevant to the checkpoint question. For
each premise:

- statement: a concise claim supported by the fact pattern
- kind: one of the valid premise kinds listed above
- source_quote: an EXACT verbatim substring of the fact pattern that
  supports this premise (required for factual premises; must appear
  word-for-word in the fact pattern text above)

Return ONLY a JSON object:
{ "draft_premises": [ { "statement": string, "kind": string, "source_quote": string }, ... ] }
