---
hook_name: version_change_summary
version: v1
schema_in:
  type: object
  required: [diff, frame_mode]
  properties:
    diff: { type: object }
    parent_version_change_summary: { type: ["string", "null"] }
    frame_mode: { enum: ["legal", "general"] }
schema_out:
  type: object
  required: [change_summary]
  properties:
    change_summary: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are writing a concise change summary for a new version of a
{{frame_mode}} argument frame.

Structural diff:
{{{diff}}}

{{#parent_version_change_summary}}
Previous version summary: {{parent_version_change_summary}}
{{/parent_version_change_summary}}

Write a change summary of under 30 words that describes what changed
in this version. Be specific (mention node types or fields changed).
Do not include node ids. Write in past tense.

Return ONLY a JSON object: { "change_summary": string }
