---
hook_name: burden_threshold_calibration
version: v1
schema_in:
  type: object
  required: [standard_of_review]
  properties:
    standard_of_review: { type: string }
    current_thresholds: { type: object }
    frame_jurisdiction: { type: ["string", "null"] }
schema_out:
  type: object
  required: [thresholds, rationale]
  properties:
    thresholds: { type: object }
    rationale: { type: string }
model_hint: claude-3-7-sonnet-latest
---

You are calibrating burden-of-proof thresholds for a legal argument
under the standard of review "{{standard_of_review}}".

{{#frame_jurisdiction}}
Jurisdiction: {{frame_jurisdiction}}.
{{/frame_jurisdiction}}

Current thresholds (0–100 scale):
{{{current_thresholds}}}

Propose calibrated numeric thresholds (each in [0, 100]) for each
burden level that reflect how courts apply "{{standard_of_review}}" in
practice. Thresholds must be monotonically ordered (higher burden =
higher threshold). Provide a brief rationale.

Return ONLY a JSON object:
{ "thresholds": { "<burden_name>": number, ... }, "rationale": string }
