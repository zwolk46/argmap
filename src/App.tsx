export function App() {
  return (
    <main data-testid="placeholder-root" style={{ padding: "2rem", fontFamily: "sans-serif" }}>
      <h1>argmap</h1>
      <p>v1 skeleton. Modules are stubs; per-module coding sessions populate them.</p>
    </main>
  );
}
